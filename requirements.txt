# Convenience export from pyproject.toml.
# Python dependencies only: the retired legacy bridge's NDI SDK / CombinedApi
# must be installed separately if that compatibility path is built manually.
numpy>=1.26
PyYAML>=6.0
pyserial>=3.5
dynamixel-sdk>=3.7
matplotlib>=3.8
PySide6>=6.7
pytest>=8.2
