Legacy bridge-only source retained for compatibility comparison.

The active tracker runtime for this project is the Python NDI backend under
`continuum_robot/tracking/ndi_backend.py`.

Everything in this directory exists only for:
- fallback/debug comparisons
- old bridge protocol compatibility
- historical bring-up reference

It is not the intended day-to-day runtime path.
