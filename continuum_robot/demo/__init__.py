"""Presentation-only demo modules for the continuum robot platform.

This package houses demo-quality utilities that are explicitly NOT thesis-grade
research code. They reuse the same safe primitives as the experiments
(ServoService, tracking snapshots, validators) but produce artifacts that are
labelled and gated as demo-only — never `valid_for_model_training` and never
`valid_for_thesis_repeatability`.
"""
